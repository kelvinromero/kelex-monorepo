type Query {
    episodes: [Episode]
    episodesByTitle(title: String!): [Episode]
    findEpisodeByFuzzyTitle(title: String!): [Episode]
}

type Episode {
    id: ID!
    title: String!
    description: String!
}